name: Rodar Docker

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código
        uses: actions/checkout@v3

      - name: Build Docker
        run: docker compose up --build -d

      - name: Verificar containers
        run: docker ps
